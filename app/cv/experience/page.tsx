import { title } from "@/app/_components/primitives";

export default function ExperiencePage() {
  return (
    <div>
      <span className={title({ color: "yellow" })}>ToDo</span>
    </div>
  );
}
